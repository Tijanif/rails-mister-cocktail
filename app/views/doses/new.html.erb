<h2>Create a dose</h2>

<%= simple_form_for([@cocktail, @dose]) do |f| %>
    <%= f.association :ingredient %>
    <%= f.input :description, :label => "Quantity", :placeholder => "TYPE HERE" %>
    <%= f.submit "add an ingredient", class: "btn btn-primary"%>
<% end %>
